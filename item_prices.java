// write a function to return the prices for all inventory items in an order (SaleItems)
// for each price I store it in the variable SaleItems[I]
// the function should take an input of J ASale.items, where
// ASale.items[J] is the price (if any)
// ASale.items[J].product_type is item type (eg. 'configurable or 'simple')
// ASale.items[J].parent_item_id is the parent item id (if any)
// ASale.items[J].item_id is the item id 
// ASale.items.sku is the indicator that the configurable comes AFTER (not before)
